# 📂 Task 2.4: Tool Page UI & Copywriting Blueprint

## 1. 页面架构逻辑 (Page Architecture)

工具页（如 /jpg-to-png）必须兼顾**功能性**（好用）和**内容性**（SEO）。

**页面区域划分：**

1. **Hero Section (Above the Fold)**: 标题 + 核心转换工具区域。这是用户唯一关心的地方。
2. **Value Props (Trust Signals)**: 为什么用我们？（安全、快速、免费）。
3. **How-to Guide (SEO)**: 文字版步骤（Google 需要读文字）。
4. **Technical Specs (Table)**: 格式的详细参数对比。
5. **FAQ (Schema)**: 针对该工具的问答。
6. **Related Tools**: 侧边栏或底部推荐（增加 PV）。

---

## 2. 功能需求列表 (Functional Requirements)

在让 AI 写代码前，明确功能边界。

- **核心转换 (Core Conversion)**:
    - 支持拖拽上传 (Drag & Drop)。
    - 支持批量上传 (Batch Upload, e.g., max 10 files)。
    - **技术路线建议**: 优先使用 **Client-side (浏览器端) 处理**。
        - *方案*: 使用 WebAssembly (如 ffmpeg.wasm 或 imagemagick.wasm)。
        - *优势*: 保护隐私（图片不上传服务器），零服务器成本，速度极快。
- **交互反馈 (UX)**:
    - 上传时的进度条。
    - 转换成功后的“Download All (ZIP)”按钮。
    - 转换前后的文件大小对比（展示压缩效果，如 "-70%"）。
- **隐私机制**:
    - 如果是服务器端处理：必须声明 "Files are deleted automatically after 2 hours"。
    - 如果是浏览器端处理：声明 "Files never leave your device"。

---

## 3. UI Copywriting Templates (文案模板)

请使用以下模板为每个工具页生成文案。

### 🎨 模板变量定义

- {Input_Format}: e.g., JPG
- {Output_Format}: e.g., PNG
- {Tool_Name}: e.g., JPG to PNG Converter

### Section 1: Hero Area

- **H1 Headline**: Convert {Input_Format} to {Output_Format} for Free
- **Sub-headline**: The fastest online converter. High quality, no file limits, and 100% secure.
- **Button CTA**: Select {Input_Format} Images
- **Micro-copy**: or drag and drop files here

### Section 2: Trust Badges (Icon + Text)

1. **Privacy First**: "All conversions happen locally. Your photos never leave your browser." (针对客户端方案)
2. **Lightning Fast**: "Powered by WebAssembly for instant results."
3. **Batch Processing**: "Convert up to 50 images at once."

### Section 3: How-to Guide (Must be H2 + Ordered List)

**H2 Title**: How to convert {Input_Format} to {Output_Format}?

1. **Step 1**: Click the **Select Files** button or drag your {Input_Format} images into the box.
2. **Step 2**: Adjust quality settings if needed, then click **Convert**.
3. **Step 3**: Wait for the process to finish and download your new {Output_Format} files individually or as a ZIP.

### Section 4: Why Convert? (SEO Text)

- **H2 Title**: Why convert {Input_Format} to {Output_Format}?
- **Content Logic**:
    - Explain the main benefit of {Output_Format} (e.g., "PNG allows transparency, while JPG does not").
    - Explain compatibility (e.g., "WebP loads faster on websites").
    - *Note to Agent: Use data from the Pillar Content (Task 2.1) here.*

### Section 5: Technical Comparison Table

| **Feature** | **{Input_Format}** | **{Output_Format}** |
| --- | --- | --- |
| **Best For** | (e.g., Photos) | (e.g., Graphics) |
| **Transparency** | No | Yes |
| **Compression** | Lossy | Lossless |

### Section 6: FAQ

- **Q**: Is it free? **A**: Yes, completely free.
- **Q**: Do I need to install software? **A**: No, it works in your browser (Chrome, Safari, Edge).
- **Q**: Can I use it on mobile? **A**: Yes, it works on iPhone and Android.

---

## 4. 给 AI Agent 的开发指令 (Prompt Strategy)

当你准备生成 UI 代码时，使用这套逻辑：

**Role**: You are a Frontend Developer specializing in React/Next.js and Tailwind CSS.

**Input**: Take the copy from the templates above.

**Task**: Create a reusable ToolPageLayout component.

**Requirements**:

1. **Semantic HTML**: Use <article>, <section>, <h1>, <h2> correctly for SEO.
2. **Responsive**: Mobile-first design using Tailwind classes.
3. **Component Props**:
    - title
    - description
    - faqItems (Array)
    - conversionFunction (The logic hook)
4. **UI Style**: Clean, minimalist (Stripe-like aesthetic). Use plenty of whitespace.